<button
  class="button{{#if fullWidth}} button--full{{/if}}{{#if isIcon}} button--icon{{/if}}{{#if className}} {{className}}{{/if}}"
  {{#if ariaLabel}}aria-label="{{ariaLabel}}"{{/if}}
>
  {{{content}}}
</button>
